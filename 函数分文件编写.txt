1. 创建.h 文件 和.cpp文件，.h 中添加 #pragma once  // 防止重复
2. 头文件中写函数声明，源文件中写函数定义。
3. 其他cpp文件引用#include "xxx.h" 直接调用函数。

类模板分文件编写： 类模板中成员函数创建时机是在调用时，导致编译时链接不到
1. 创建.h 文件 和.cpp文件 .h 中添加 #pragma once  // 防止重复
2. 头文件中写类模板声明，源文件中写函数类外实现.
解决办法：把.h 和 .cpp 文件结合一起，后缀名改为 .hpp 文件。